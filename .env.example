# Copy to .env.local for local dev. Cloud sync is optional — without this the
# app stores stats in localStorage only.
#
# Base URL of your self-hosted sync API (no trailing slash).
VITE_API_URL=https://api.isayenko.org
