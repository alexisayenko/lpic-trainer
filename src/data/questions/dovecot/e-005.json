{
  "id": "dovecot-e005",
  "tool": "dovecot",
  "objective": "211.3",
  "prompt": "As a system administrator for a mid-sized company, you are requested to enforce logging for authentication failures in the Dovecot email server. Which directive in the `dovecot.conf` file allows you to log these authentication attempts?",
  "choices": [
    "`auth_debug = yes`",
    "`log_auth_failures = yes`",
    "`auth_verbose = yes`",
    "`verbose_auth = yes`"
  ],
  "answerIndex": 2,
  "explanation": "The `auth_verbose = yes` directive in `dovecot.conf` enables verbose logging of authentication attempts, including failures, helping administrators monitor login problems.",
  "origin": "linux-direct"
}
