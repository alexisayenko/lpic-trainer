{
  "id": "bind9-a008",
  "tool": "bind9",
  "prompt": "Some users are unable to connect to specific local hosts by name, while access to hosts in other zones works as expected. Hosts are reachable by IP. What is the default log file that could provide hints about the problem?",
  "choices": [
    "`/var/log/named.debug`",
    "`/var/log/messages`",
    "`/var/log/dns.log`",
    "`/etc/named.log`"
  ],
  "answerIndex": 1,
  "explanation": "By default BIND logs to syslog, which on most systems writes to `/var/log/messages`, the place to look for name resolution errors."
}
