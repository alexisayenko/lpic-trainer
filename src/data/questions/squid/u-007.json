{
  "id": "squid-u007",
  "tool": "squid",
  "objective": "208.3",
  "prompt": "Only clients on the `192.168.1.0/24` network should be able to use the proxy. Which configuration section is correct?",
  "choices": [
    "`acl local src 192.168.1.0/24` then `http access allow local`",
    "`acl local src 192.168.1.0/24` then `http_access_allow = local`",
    "`acl local src 192.168.1.0/24` then `http_allow local`",
    "`acl local src 192.168.1.0/24` then `http_access allow local`",
    "`acl local src 192.168.1.0/24` then `httpd local allow`"
  ],
  "answerIndex": 3,
  "explanation": "Define the network with `acl`, then permit it with `http_access allow <aclname>`.",
  "origin": "ken-adams"
}
