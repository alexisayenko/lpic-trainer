# Copy to .env.local for local dev. Cloud sync is optional — without these the
# app stores stats in localStorage only.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
