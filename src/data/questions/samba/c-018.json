{
  "id": "samba-c018",
  "tool": "samba",
  "prompt": "Which of the following Samba services handles the membership of a file server in an Active Directory domain?",
  "choices": [
    "`nmbd`",
    "`smbd`",
    "`samba`",
    "`winbindd`"
  ],
  "answerIndex": 3,
  "explanation": "The `winbindd` daemon resolves users and groups from the Active Directory domain, so it handles a file server membership in an AD domain. The `samba` daemon is used when running Samba itself as an AD domain controller."
}
