{
  "id": "bind9-a012",
  "tool": "bind9",
  "objective": "207.1",
  "prompt": "A zone file on the DNS server has been updated. Which command will tell `named` to reload its zone files?",
  "choices": [
    "`named -r`",
    "`killall -HUP named`",
    "`dig +reload`",
    "`service named flush`"
  ],
  "answerIndex": 1,
  "explanation": "Sending the HUP signal to `named` (`killall -HUP named`) causes it to reread its configuration and zone files; `rndc reload` achieves the same.",
  "origin": "linux-direct"
}
