{
  "id": "dovecot-u001",
  "tool": "dovecot",
  "objective": "211.3",
  "prompt": "After installing Dovecot, `ps ax` shows processes only as `dovecot/imap` and `dovecot/imap-login`. Which option associates each process with the username, peer IP address, and connection status?",
  "choices": [
    "`verbose_proctitle = yes` in the Dovecot configuration",
    "`proc.all.show_status = 1` in `sysctl.conf` or `/proc`",
    "`sys.ps.allow_descriptions = 1` in `sysctl.conf` or `/proc`",
    "`--with-linux-extprocnames` for ./configure when building Dovecot",
    "`process_format = \"%u %I %s\"` in the Dovecot configuration"
  ],
  "answerIndex": 0,
  "explanation": "Setting `verbose_proctitle = yes` makes Dovecot include the user, peer IP, and connection state in each process title shown by `ps`.",
  "origin": "ken-adams"
}
