{
  "id": "apache2-u032",
  "tool": "apache2",
  "prompt": "A web server handling ~200 simultaneous requests runs slowly during occasional spikes. Which httpd.conf directives should be adjusted?",
  "choices": [
    "`MinSpareServers` & `MaxSpareServers`",
    "`MinSpareServers`, `MaxSpareServers`, `StartServers` & `MaxClients`",
    "`MinSpareServers`, `MaxSpareServers`, `StartServers`, `MaxClients` & `KeepAlive`",
    "`MinServers`, `MaxServers` & `MaxClients`"
  ],
  "answerIndex": 1,
  "explanation": "Tuning the process pool (`StartServers`, `Min`/`MaxSpareServers`) and the concurrency cap (`MaxClients`) addresses load spikes.",
  "origin": "ken-adams"
}
