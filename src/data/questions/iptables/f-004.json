{
  "id": "iptables-f004",
  "tool": "iptables",
  "prompt": "Laura is working as a system administrator for a company. She is asked to ensure that any incoming traffic on port 8080 is redirected to port 80 on the same server. Which iptables rule will achieve this?",
  "choices": [
    "iptables -A INPUT -p tcp --dport 8080 -j REDIRECT --to-port 80",
    "iptables -A FORWARD -p tcp --dport 8080 -j DNAT --to 80",
    "iptables -A OUTPUT -p tcp --sport 8080 -j REDIRECT --to-port 80",
    "iptables -A INPUT -p tcp --dport 80 -j REDIRECT --to-port 8080"
  ],
  "answerIndex": 0,
  "explanation": "The REDIRECT target with --to-port 80 redirects traffic arriving on port 8080 to port 80 on the local machine."
}
