{
  "id": "samba-c001",
  "tool": "samba",
  "prompt": "A systems administrator wants to configure the Samba server settings. Which file in the /etc/samba/ directory primarily holds the global and share-specific configurations for Samba?",
  "choices": [
    "/etc/samba/smb.conf",
    "/etc/samba/samba.conf",
    "/etc/samba/shares.conf",
    "/etc/samba/global.conf"
  ],
  "answerIndex": 0,
  "explanation": "smb.conf is the main Samba configuration file, containing the [global] section and per-share sections."
}
