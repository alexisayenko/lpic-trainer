{
  "id": "nfs-c023",
  "tool": "nfs",
  "prompt": "An administrator wants the root user on an NFS client to keep root privileges on a share instead of being mapped to the anonymous user. Which `/etc/exports` option enables this?",
  "choices": [
    "`sync`",
    "`nfsvers=3`",
    "`no_root_squash`",
    "`subtree_check`"
  ],
  "answerIndex": 2,
  "explanation": "`no_root_squash` disables the default root-squashing, so the root user on a client keeps root privileges on the exported share instead of being mapped to the anonymous user.",
  "origin": "linux-direct"
}
